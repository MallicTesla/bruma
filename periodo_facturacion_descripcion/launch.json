{
    "version": "0.2.0",
    "configurations": [
        {
        "name": "Debug Odoo",
        "type": "python",
        "request": "launch",
        "program": "${workspaceFolder}/odoo-bin",
        "args": [
            "-d", "bruma03",
            "--log-level=debug",
            "--dev=all"
        ],
        "console": "integratedTerminal",
        "cwd": "${workspaceFolder}"
        }
    ]
}